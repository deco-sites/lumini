import Component from "$store/components/ui/OurStores.tsx";
import type { Props } from "$store/components/ui/OurStores.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
